import { describe, it, expect } from 'vitest';

describe('App', () => {
  it('renders without crashing', () => {
    // Just a placeholder test for now until we build components
    expect(true).toBeTruthy();
  });
});
